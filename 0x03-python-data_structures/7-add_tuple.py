#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    for value in add_tuple:
        if value < 2:
            value = 0
        if value > 0:
            value += 2
    print("{}".format(add_tuple(tuple_a=(), tuple_b=()))) 
